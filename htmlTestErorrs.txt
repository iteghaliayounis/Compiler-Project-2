{% extends "base.html" %}

{% block title %}Products{% endblock %}

{% block content %}

<div style="display:grid;grid-template-columns:repeat(auto-fill, minmax(230px, 1fr));gap:20px;">

    {% for p in products %}
    <div style="background:white;padding:15px;border-radius:10px;">

        <div style="font-weight:bold;margin-top:10px;">
            {{ p.name }}
        </div>

        <div style="color:#0ea5e9;font-weight:bold;">
            {{ p.price }}
        </div>

        <!-- خطأ 1: UNDEF_VAR — المتغير undefined_var مش موجود لا ببايثون ولا بالجينجا -->
        <div>{{ undefined_var }}</div>

        <!-- خطأ 2: UNDEF_VAR — category مش معرّفة بالبايثون ولا بالـ for loop -->
        <div>{{ category }}</div>

        <a href="{{ url_for('product_detail', pid=p.id) }}"
           class="btn secondary"
           style="display:inline-block;margin-top:8px;">
        </a>

    </div>
    {% endfor %}

</div>

<!-- خطأ 3: UNDEF_VAR — cart_count مش معرّفة بأي مكان -->
<div>Cart: {{ cart_count }}</div>

{% endblock %}